# コーディング規約

- Deno 標準スタイルガイドと TypeScript `strict` モード遵守、暗黙的な `any`
  を禁止。
- import は `import_map.json` 経由を推奨し、相対パス乱用を避ける。
- Conventional Commits 形式のコミットメッセージ/PR タイトルを推奨。
- Slack API 依存テストはモック利用で安定化を図る方針。
